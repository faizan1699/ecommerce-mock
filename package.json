{
  "name": "json-ecommerce-store",
  "version": "1.0.0",
  "description": "Ecommerce store with HTML/CSS/JS + json-server (auth, admin, cart, JazzCash/EasyPaisa dummy checkout)",
  "scripts": {
    "server": "json-server --watch db.json --port 3000",
    "web": "http-server -p 5500 -c-1",
    "dev": "concurrently -n API,WEB -c blue,green \"npm run server\" \"npm run web\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "http-server": "^14.1.1",
    "json-server": "^0.17.4"
  }
}
